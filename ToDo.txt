- fix display bug in inventory overlays
- make the inventory item a string (the whole inventory is a list (ul, li))

update systemScamatic

connect saveSystem to the save button
make multiple save slots