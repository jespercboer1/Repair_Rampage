style the save file content

don't hardcode what data is available in save system, if not available use Null or 0 etc.





possible settings:
- configurable colors (stats + ui)
- popup switch (new save + overwrite + delete + clear (all separate))
- autosaves switch + interval
- Reset
- 